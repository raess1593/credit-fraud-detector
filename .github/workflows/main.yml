name: Main Workflow

on:
    push:
        branches: [main]
    pull_request:
        branches: [main]
    workflow_dispatch:

jobs:
    call-lint:
        uses: ./.github/workflows/lint.yml

    call-test:
        needs: call-lint
        uses: ./.github/workflows/test.yml